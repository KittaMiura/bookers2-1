<div class="container mt-3">
  <div class="container px-5 px-sm-0">
  <div class="row">
    
    <%= render 'list', books: @books %>
    
    <div class="col-md-8 offset-md-1">
      <h2>Book detail</h2>
      <div>
        <p>ショップ名：<%= @book.title %></p>
        <p>説明：<%= @book.opinion %></p>
        <p>ユーザーネーム：<%= @book.user.name %></p>
        <p>投稿日：<%= @book.created_at.strftime('%Y/%m/%d') %></p>
      </div>
    </div>
  </div>
  </div>
  </div>